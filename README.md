# ASP.NET Core Микросервисы с Clean Architecture

Это полноценный обучающий проект, демонстрирующий создание микросервисной архитектуры на ASP.NET Core с применением лучших практик и паттернов.

## 🎯 Цель проекта

Проект создан как образовательный материал для изучения:
- Микросервисной архитектуры
- Clean Architecture (Чистая архитектура)
- CQRS паттерна с MediatR
- Асинхронной коммуникации через RabbitMQ и MassTransit
- Entity Framework Core с SQL Server
- FluentValidation
- Docker контейнеризации

## 📐 Архитектура проекта

### Микросервисы

Система состоит из следующих сервисов:

1. **Catalog Service** - Управление каталогом товаров
2. **Order Service** - Управление заказами (в разработке)
3. **BuildingBlocks** - Общие библиотеки и контракты

### Clean Architecture

Каждый микросервис построен по принципам Clean Architecture с 4 слоями:

```
┌─────────────────────────────────────────┐
│         API (Presentation)              │  ← Контроллеры, Middleware
├─────────────────────────────────────────┤
│         Application                     │  ← CQRS, MediatR, Валидация
├─────────────────────────────────────────┤
│         Domain                          │  ← Entities, Бизнес-логика
├─────────────────────────────────────────┤
│         Infrastructure                  │  ← EF Core, Репозитории, БД
└─────────────────────────────────────────┘
```

**Правило зависимостей:** Зависимости направлены внутрь. Domain ни от чего не зависит.

## 📂 Структура проекта

```
MicroservicesApp/
├── src/
│   ├── Services/
│   │   ├── Catalog/
│   │   │   ├── Catalog.Domain/          # Entities, Value Objects, Events
│   │   │   ├── Catalog.Application/     # Commands, Queries, DTOs
│   │   │   ├── Catalog.Infrastructure/  # DbContext, Repositories
│   │   │   └── Catalog.API/            # Controllers, Program.cs
│   │   └── Order/                       # Аналогичная структура
│   └── BuildingBlocks/
│       └── BuildingBlocks.Messaging/    # Integration Events
├── docker-compose.yml
└── MicroservicesApp.sln
```

## 🛠 Технологии

- **.NET 8.0** - Платформа разработки
- **ASP.NET Core Web API** - REST API
- **Entity Framework Core** - ORM для работы с БД
- **SQL Server** - Реляционная база данных
- **MediatR** - Реализация паттерна Mediator для CQRS
- **FluentValidation** - Валидация запросов
- **RabbitMQ** - Message broker для асинхронной коммуникации
- **MassTransit** - Фреймворк для работы с RabbitMQ
- **Docker** - Контейнеризация
- **Serilog** - Структурированное логирование
- **Swagger/OpenAPI** - Документация API

## 🎓 Используемые паттерны

### 1. CQRS (Command Query Responsibility Segregation)

Разделение операций чтения и записи:

**Command (Команда):**
```csharp
public record CreateProductCommand(
    string Name,
    decimal Price) : IRequest<Result<ProductDto>>;
```

**Query (Запрос):**
```csharp
public record GetProductsQuery : IRequest<IEnumerable<ProductDto>>;
```

### 2. Repository Pattern

Абстракция для работы с данными:

```csharp
public interface IRepository<T> where T : BaseEntity
{
    Task<T?> GetByIdAsync(Guid id);
    Task<IEnumerable<T>> GetAllAsync();
    Task<T> AddAsync(T entity);
}
```

### 3. Unit of Work Pattern

Координация работы нескольких репозиториев:

```csharp
public interface IUnitOfWork
{
    IProductRepository Products { get; }
    Task<int> SaveChangesAsync();
    Task BeginTransactionAsync();
    Task CommitTransactionAsync();
}
```

### 4. Domain Events

События в рамках домена:

```csharp
public record ProductCreatedEvent(Guid ProductId, string ProductName) : IDomainEvent;
```

### 5. Integration Events

События для коммуникации между микросервисами:

```csharp
public record ProductPriceChangedIntegrationEvent
{
    public Guid ProductId { get; init; }
    public decimal NewPrice { get; init; }
}
```

## 🚀 Запуск проекта

### Предварительные требования

- Docker Desktop
- .NET 8.0 SDK (опционально, для локальной разработки)

### Запуск через Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd ProjectLaravel
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Проверьте статус контейнеров:
```bash
docker-compose ps
```

### Доступ к сервисам

После запуска будут доступны:

- **Catalog API:** http://localhost:5001
- **Swagger UI:** http://localhost:5001/swagger
- **SQL Server:** localhost:1433 (sa / YourStrong@Passw0rd)
- **RabbitMQ Management:** http://localhost:15672 (guest / guest)
- **Seq (Logs):** http://localhost:5341

## 📊 Примеры использования API

### Создать продукт (POST /api/products)

```bash
curl -X POST http://localhost:5001/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Laptop",
    "description": "Gaming laptop",
    "price": 1500.00,
    "stock": 10,
    "imageUrl": "https://example.com/laptop.jpg",
    "categoryId": "00000000-0000-0000-0000-000000000000"
  }'
```

### Получить все продукты (GET /api/products)

```bash
curl http://localhost:5001/api/products
```

### Health Check (GET /api/products/health)

```bash
curl http://localhost:5001/api/products/health
```

## 🧪 Как это работает

### Поток создания продукта (CQRS Command)

1. **API Controller** получает запрос
2. Создает **Command** объект
3. Отправляет Command в **MediatR**
4. **ValidationBehavior** валидирует данные (FluentValidation)
5. **CommandHandler** обрабатывает команду:
   - Создает **Domain Entity** через фабричный метод
   - Сохраняет в **Repository**
   - Коммитит через **Unit of Work**
6. Возвращает **Result<DTO>** клиенту

```
API → Command → MediatR → Validation → Handler → Domain → Repository → DB
```

### Поток получения продуктов (CQRS Query)

1. **API Controller** получает запрос
2. Создает **Query** объект
3. Отправляет Query в **MediatR**
4. **QueryHandler** получает данные из **Repository**
5. Преобразует Entities в **DTOs**
6. Возвращает данные клиенту

```
API → Query → MediatR → Handler → Repository → DB → DTOs → API
```

## 📚 Дальнейшее развитие

Проект можно расширить:

- ✅ Добавить Order Service
- ✅ Настроить MassTransit для межсервисной коммуникации
- ✅ Добавить API Gateway (Ocelot/YARP)
- ✅ Реализовать аутентификацию и авторизацию (JWT)
- ✅ Добавить кэширование (Redis)
- ✅ Настроить мониторинг (Prometheus + Grafana)
- ✅ Добавить интеграционные тесты
- ✅ Реализовать Circuit Breaker паттерн (Polly)

## 🎯 Основные концепции

### Что такое Clean Architecture?

Clean Architecture - это подход к организации кода, который:
- Делает систему независимой от фреймворков
- Упрощает тестирование
- Разделяет бизнес-логику от инфраструктуры
- Следует принципу инверсии зависимостей (DIP)

### Что такое CQRS?

CQRS разделяет операции:
- **Commands** - изменяют состояние системы
- **Queries** - только читают данные

Это позволяет:
- Оптимизировать чтение и запись отдельно
- Упростить модели
- Масштабировать независимо

### Что такое MediatR?

MediatR - библиотека, реализующая паттерн Mediator:
- Убирает прямые зависимости между объектами
- Организует обработку команд и запросов
- Поддерживает Pipeline Behaviors (middleware)

### Что такое Domain-Driven Design (DDD)?

DDD - подход к разработке, где:
- Бизнес-логика находится в Domain слое
- Entities имеют поведение, а не только данные
- Используются Value Objects для инкапсуляции
- Domain Events для реакции на изменения

## 📖 Полезные ссылки

- [Clean Architecture by Robert C. Martin](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
- [CQRS Pattern](https://martinfowler.com/bliki/CQRS.html)
- [MediatR Documentation](https://github.com/jbogard/MediatR)
- [MassTransit Documentation](https://masstransit-project.com/)
- [Entity Framework Core](https://docs.microsoft.com/ef/core/)

## 👨‍💻 Автор

Создано как образовательный материал для изучения микросервисной архитектуры на ASP.NET Core.

## 📝 Лицензия

MIT License - используйте свободно для обучения и разработки.

---

**Приятного обучения! 🚀**
